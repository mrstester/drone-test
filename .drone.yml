kind: pipeline
type: docker
name: default

steps:
- name: hello
  image: alpine:latest
  commands:
  - echo "Hello from Drone CI!"
  - echo "Repo: $DRONE_REPO"
  - echo "Branch: $DRONE_BRANCH"

trigger:
  branch:
  - main
